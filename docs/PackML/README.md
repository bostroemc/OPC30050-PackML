# OPC 30050 - OPC UA for PackML

Read the specification: <https://bostroemc.github.io/OPC30050-PackML/PackML/>

This directory is generated by `Opc.Ua.SpecificationPublisher publish` from the STS XML, and it is what GitHub Pages serves. Nothing here is edited by hand - the next build discards it. Edit `source/PackML/` or `model/`, and the workflow rebuilds this.
