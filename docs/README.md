# OPC30050-PackML

Read the specifications: <{"message":"Not Found","documentation_url":"https://docs.github.com/rest/pages/pages#get-a-apiname-pages-site","status":"404"}>

| Specification | Where |
|---|---|
| OPC 20020 - OPC UA for Sample Applications | [`Sample/`](Sample/index.html) |

This directory is generated by `Opc.Ua.SpecificationPublisher publish` from the STS XML, and it is what GitHub Pages serves. Nothing here is edited by hand - the next build discards it. Edit `source/`, `model/` or a specification's `manifest.json`, and the workflow rebuilds this.
